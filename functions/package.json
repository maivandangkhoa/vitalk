{
  "name": "havitalk-functions",
  "private": true,
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.80.0",
    "cheerio": "^1.2.0",
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.3.2",
    "googleapis": "^171.4.0",
    "nodemailer": "^8.0.4"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/nodemailer": "^7.0.11",
    "typescript": "~5.9.3"
  }
}
