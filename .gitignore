# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.local
.env.production

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase
.firebase/
firebase-auth.json

# TURN server — the live config holds the shared auth secret, and the certs are
# copies of Caddy's private key. Only the .example is tracked.
infrastructure/coturn/turnserver.conf
infrastructure/coturn/certs/
